# 已开发功能与项目特点

本文档用于记录当前已经实现或已具备代码基础的功能，便于团队同步进度、写报告、做答辩演示和后续迭代。新增功能完成后，请同步更新本文档；未完成或计划中的事项请写入 `TODO.md`。

## 项目特点

- **主题频道交流场景**：围绕主题频道组织用户、帖子、评论和收藏，适合兴趣交流、专题讨论和社区管理。
- **前后端分离规划 + Web 端优先落地**：README 中规划 Web 后端、Web 前端、Android APP 和 MySQL 数据库；当前仓库已包含 Spring Boot Web 后端和 Thymeleaf 页面模板。
- **分层结构清晰**：后端按 Controller、Service、Repository、Entity 分层，便于后续扩展频道、帖子、评论、审核等业务。
- **审核状态建模**：频道、主题帖、评论服务层已具备“待审核/审核通过/审核未通过”的状态流转基础。
- **核心领域对象较完整**：已覆盖 User/OrdinaryUser、Administrator、Forum、ForumMember、ThemePost、Comment、Collect 等实体。

## 当前已具备的功能基础

### 1. 应用入口与首页

- Spring Boot 应用入口已建立，可启动 `TopicChannelPlatformApplication`。
- 根路径 `/` 映射到首页模板 `index.html`。

### 2. 用户中心

- **注册页面与注册逻辑**：支持手机号、用户名、密码、确认密码提交；后端会校验两次密码一致，并阻止重复手机号注册。
- **登录页面与登录逻辑**：支持手机号 + 密码登录，登录成功后在 Session 中保存 `userId`。
- **个人信息页**：登录用户可访问个人信息详情页；未登录访问会跳转登录页。
- **个人信息修改**：支持修改用户名、真实姓名、性别、生日、身份证号等资料。
- **修改密码**：登录用户可使用旧密码修改新密码，并校验两次新密码一致。
- **找回/重置密码**：支持通过手机号重置密码的基础逻辑。
- **账号注销**：支持删除当前登录用户并清理 Session。

### 3. 频道管理服务层

- 支持创建频道，并自动写入创建时间与“待审核”状态。
- 支持按 ID 查询频道、查询全部频道、按频道名称关键字筛选。
- 支持修改频道名称/简介，并将修改后的频道重新置为“待审核”。
- 支持更新频道审核状态和删除频道。

### 4. 频道成员服务层

- 支持用户加入频道，并记录加入时间。
- 支持用户退出频道。
- 支持查询用户加入的频道。
- 支持判断用户是否已加入指定频道。

### 5. 主题帖服务层

- 支持发布主题帖，并自动写入发布时间与“待审核”状态。
- 支持按帖子 ID 查询、按频道 ID 查询帖子。
- 支持编辑帖子标题和正文。
- 支持更新帖子审核状态和删除帖子。

### 6. 评论服务层

- 支持发表评论，并自动写入发布时间与“待审核”状态。
- 支持按主题帖 ID 查询评论。
- 支持删除评论。
- 支持更新评论审核状态。

### 7. 收藏服务层

- 支持收藏主题帖，并记录收藏时间。
- 支持取消收藏。
- 支持查询用户收藏列表。
- 支持判断用户是否已收藏指定帖子。

### 8. 管理员与审核服务层

- 支持管理员登录校验。
- 支持查询全部普通用户和按关键字搜索用户。
- 支持管理员审核频道、主题帖和评论，并写入“审核通过”或“审核未通过”状态。

### 9. 数据持久化与页面模板

- 使用 Spring Data JPA 访问 MySQL。
- 已配置 MySQL 数据源、Hibernate 自动更新表结构和 Thymeleaf 模板路径。
- 已包含登录、注册、个人信息详情、个人信息编辑、修改密码、找回密码、首页等页面模板。

## 建议的记录规范

新增或完成一个功能时，可以按以下格式记录：

```markdown
### 功能名称

- 功能说明：一句话说明用户能做什么。
- 入口路径：页面 URL 或接口路径。
- 关键文件：Controller / Service / Entity / Template。
- 验收状态：已完成 / 部分完成 / 待联调。
- 备注：限制、待优化点或演示注意事项。
```

如果功能只是计划、缺陷或优化事项，请记录在 `TODO.md`，不要放入已开发功能清单。
